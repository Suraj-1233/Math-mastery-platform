
# ─────────────────────────────────────────────────
# 🛡️ Math Mastery Platform - Environment Variables
# ─────────────────────────────────────────────────
# Copy this file to .env.local and fill in all values
# ⚠️ NEVER commit .env or .env.local to git

# ─── Database ───────────────────────────────────
# Development: SQLite
# DATABASE_URL="file:./dev.db"
# Production: Use PostgreSQL (Prisma supports both)
DATABASE_URL="file:./dev.db"

# ─── NextAuth / Auth.js ─────────────────────────
# Generate a strong secret: openssl rand -base64 32
AUTH_SECRET="CHANGE_ME_TO_A_SECURE_RANDOM_STRING_32_CHARS"
NEXTAUTH_URL="http://localhost:4000"

# ─── Google OAuth ───────────────────────────────
# Get from https://console.cloud.google.com
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ─── Email Service (Resend) ─────────────────────
# Get from https://resend.com
RESEND_API_KEY="re_xxxxxxx"

# ─── App URL ────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:4000"

# ─── Seed Endpoint Security ─────────────────────
# Generate a strong random token: openssl rand -hex 32
SEED_TOKEN="CHANGE_ME_TO_RANDOM_TOKEN"
# Set to 'true' only once to seed, then remove
ALLOW_SEED="false"
